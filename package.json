{
  "name": "mini-dissonance-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Backend-only Node.js + TypeScript project for normalizing behavioral events.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "ts-node src/index.ts",
    "start": "ts-node src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.12.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
